# Core dependencies
numpy
python-dotenv

# LLM Analysis
groq

# Audio processing
sounddevice
soundfile

# Google Cloud STT
google-cloud-speech

# Speech analysis
librosa

# Noise reduction
noisereduce

# UI visualization and real-time audio streaming
pygame
pyaudio

# PDF report generation
reportlab

# Dialect Analysis (AI Model) - Required for dialect detection
# These packages are needed for the integrated dialect detection system
# Uncomment the lines below to install:
# transformers>=4.30.0
# torch>=2.0.0

# Additional packages for model training (optional, only if you train the model yourself)
# datasets>=2.14.0
# accelerate>=0.20.0
# evaluate>=0.4.0
# scikit-learn>=1.3.0

# Installation instructions:
# 1. For dialect detection only: pip install transformers torch
# 2. For full model training: uncomment all lines above and run 'pip install -r requirements.txt'
#
# Note: The dialect detection model requires ~1.2GB disk space and ~1.5GB RAM when loaded
